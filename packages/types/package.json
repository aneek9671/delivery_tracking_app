{
  "name": "@delivery-tracker/types",
  "version": "1.0.0",
  "description": "Shared TypeScript definitions for the delivery tracker",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
